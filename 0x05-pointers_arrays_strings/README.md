# **C - Pointers, arrays and strings**

*
 > This is my tasks on C - pointers, arrays and strings, from the alx full stack engineering course.

 - All the tasks were done without the use of the standard library nor was any of the functions like printf, puts, etc. used
 - The tasks were completed without the use of the global variables
 - The tasks were done with the vi editor
 - All codes are in betty style
 - Function prototypes were provided and used in the tasks
 

### ***tasks***


| task | name | discription |
| --- | --- | --- |
| 0. | reset to 98 | a funtion that takes a pointer to an int as parameter and updates the value it poits to to 98 |
| 1. | swap | a funtion that swaps the value of two integers |
| 2. | strlen | a function return the lngth of a string |
| 3. | puts | a funtion that prints a string, followed by a new line, to stdout |
| 4. | print rev | a funtion that prints a string, in reverse |
| 5. | rev string | a function that reverses a string |
| 6. | puts2 | a function that prints every other charactor of a string, starting with the first character |
| 7. | puts half | a funtion that prints half of a string |
| 8. | print array | a function that prints n elements of an array of integers |
| 9. | strcpy | a funtion that xopies the string ppointedby src, including the terminating null byte to the buffer pointd to by dest |



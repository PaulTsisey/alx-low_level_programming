#include <stdio.h>
#include <stdlib.h>

/**
 *main - prints all single digit numbers of base 10 from 0
 *followed by a new line
 *Return:always 0
 */

int main(void)
{
	int p;

	p = '0';
	while
		(p < '10')
		{
			putchar('p');
			p++;
		}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 *main - prints in lower case then upper case
 * followed by a nwe line
 *Return: always 0
 */

int main(void)
{
	char uc;
	char lc;

	uc = 'A';
	lc = 'a';
	while
		(lc <= 'z')
	{
		putchar(lc);
		lc++;
	}
	while
		(UC <= 'Z')
	{
		putchar(UC);
		UC++;
	}
	putchar(\n);
	return (0);
}

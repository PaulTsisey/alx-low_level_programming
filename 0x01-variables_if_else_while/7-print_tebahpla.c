#include <stdio.h>
#include <stdlib.h>

/**
 *main - prints lowercase alphabets in reverse
 *followed by a new line
 *Return:always 0
 */

int main(void)
{
	char p;

	p = 'z';
	while
		(p >= 'a')
		{
			putchar(p);
			p--;
		}
	putchar('\n');
	return (0);
}
